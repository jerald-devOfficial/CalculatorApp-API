[build]
  command = "npm run build"
  functions = "functions"
  publish = "build"

[functions]
  node_bundler = "esbuild"
  external = ["pg-native"]

[dev]
  command = "npm start"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200
